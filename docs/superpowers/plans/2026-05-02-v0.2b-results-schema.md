# v0.2b — Results Grid + Schema Tree — Implementation Plan

**Spec:** `docs/superpowers/specs/2026-05-02-v0.2b-results-schema-design.md`

**Goal:** Continue the written roadmap after v0.2a. Add a read-only schema sidebar and modest results-grid polish, with headless tests first and no UI automation/clicking.

## Current Baseline

- `main` includes v0.2a app shell/editor/results work.
- Latest v0.2a hardening commits include:
  - `13fbbba test(app): add ui smoke harness`
  - `a260a11 test(app): add headless app state smoke`
  - `83a4f03 fix(app): reconnect previous connection on transport errors`
- Known local blocker: macOS UI-testing permission. Do not rely on UI automation until Omar validates locally.

## Task 1: Schema metadata models

**Goal:** Add small value types for schemas, relations, and columns.

**Files:**
- Create: `Sources/LithePGCore/SchemaMetadata.swift`
- Create: `Tests/LithePGCoreTests/SchemaMetadataTests.swift`

Steps:
- [ ] Define `DatabaseSchema`, `SchemaRelation`, `SchemaColumn`, and relation kind (`table`, `view`, maybe `materializedView` later).
- [ ] Unit-test deterministic sorting: schemas alphabetically, relations alphabetically, columns by ordinal position.
- [ ] Run `swift test`.
- [ ] Commit.

## Task 2: Headless schema introspection query

**Goal:** Query Postgres catalogs/read-only metadata and map rows into the schema model.

**Files:**
- Create: `Sources/LithePGCore/SchemaIntrospector.swift`
- Create/modify: `Tests/LithePGCoreTests/SchemaIntrospectorTests.swift`

Steps:
- [ ] Write the catalog query using `information_schema.columns` plus table/view metadata.
- [ ] Exclude system schemas by default: `pg_catalog`, `information_schema`, `pg_toast`.
- [ ] Add a `POSTGRES_TEST_URL`-gated live test that creates a temp/dogfood table or uses a known public table and verifies columns.
- [ ] Run default `swift test` and live Docker smoke.
- [ ] Commit.

## Task 3: AppState schema refresh

**Goal:** Make schema loading part of the app state machine without blocking connection or query execution.

**Files:**
- Modify: `Sources/LithePGApp/AppState.swift`
- Modify: `Tests/LithePGAppTests/AppStateTests.swift`

Steps:
- [ ] Add `schema`, `isLoadingSchema`, and `schemaError` to `AppState`.
- [ ] Add `refreshSchema()`.
- [ ] Trigger refresh after successful connect, but keep errors non-fatal.
- [ ] Add headless tests for refresh success and schema error isolation.
- [ ] Run full and live tests.
- [ ] Commit.

## Task 4: Schema sidebar UI

**Goal:** Render schema metadata in the app without adding persistence or mutation.

**Files:**
- Create: `Sources/LithePGApp/SchemaSidebar.swift`
- Modify: `Sources/LithePGApp/WorkspaceView.swift`

Steps:
- [ ] Render schemas → tables/views → columns with type labels.
- [ ] Add a Refresh button bound to `AppState.refreshSchema()`.
- [ ] Show loading and error states.
- [ ] Keep interaction read-only for v0.2b unless Omar approves query insertion.
- [ ] Build and run tests.
- [ ] Commit.

## Task 5: Results grid polish

**Goal:** Improve clarity without overbuilding a full grid component yet.

**Files:**
- Modify: `Sources/LithePGApp/ResultsTable.swift`
- Modify/add tests where feasible.

Steps:
- [ ] Make empty result, command status, row count, elapsed time, and truncation status clearer.
- [ ] Preserve duplicate-column handling and accessibility identifiers.
- [ ] Consider copy-friendly cell text as a small helper, not full export.
- [ ] Run full tests.
- [ ] Commit.

## Task 6: Dogfood receipt

**Goal:** Record the v0.2b evidence.

**Files:**
- Modify: `docs/dogfood-log.md`

Steps:
- [ ] Record `swift test` result.
- [ ] Record `POSTGRES_TEST_URL` live schema smoke result.
- [ ] Leave manual UI receipt pending until Omar checks locally.
- [ ] Commit.

## Follow-up Questions for Omar

1. Hide system schemas by default? Recommendation: yes, with a later toggle.
2. Should table click insert `SELECT * FROM schema.table LIMIT 100;` into the editor? Recommendation: not in Task 4; add after sidebar renders reliably.
3. Should CSV export be part of v0.2b results polish? Recommendation: defer until schema sidebar is in.
