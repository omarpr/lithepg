# v0.3 — Dogfood-Ready — Implementation Plan

**Spec:** `docs/superpowers/specs/2026-05-03-v0.3-dogfood-ready-design.md`

**Workflow:** Direct commits to `main`; no PRs unless Omar asks.

## Current Baseline

- `main` is tagged `v0.2` and CI is green.
- v0.2 shipped query editor highlighting, schema tree, results pagination, query tabs, shortcuts, and binary-size measurement.

## Task 1: Persistence model/service contracts

**Goal:** Create testable model and protocol boundaries before wiring SwiftData/Keychain.

Files:
- Create `Sources/LithePGApp/PersistenceModels.swift`
- Create `Sources/LithePGApp/PersistenceStores.swift`
- Create `Tests/LithePGAppTests/PersistenceModelTests.swift`

Steps:
- [x] Define `ConnectionEnvironment`, `SavedConnectionMetadata`, and `QueryHistoryEntry` value types.
- [x] Define `SavedConnectionStore`, `CredentialStore`, and `QueryHistoryStore` protocols.
- [x] Add in-memory implementations for tests and first UI wiring.
- [x] Add tests proving metadata does not expose password values.
- [x] Run `swift test`.
- [x] Commit directly to `main`.

## Task 2: Saved connection AppState wiring

**Goal:** Let AppState load/select saved connections without persisting secrets in state snapshots.

Files:
- Modify `Sources/LithePGApp/AppState.swift`
- Modify/add tests in `Tests/LithePGAppTests/AppStateTests.swift`

Steps:
- [x] Inject persistence dependencies with safe defaults.
- [x] Add saved connection list state and load/add/delete methods.
- [x] Add connect-from-saved path that resolves password through `CredentialStore`.
- [x] Add tests using in-memory stores.
- [x] Run `swift test`.
- [x] Commit directly to `main`.

## Task 3: Connect sheet save flow + environment picker

**Goal:** Save connection metadata during connect with a visible environment label.

Files:
- Modify `Sources/LithePGApp/ConnectSheet.swift`
- Modify `Sources/LithePGApp/WorkspaceView.swift` if needed

Steps:
- [x] Add “Save connection” checkbox and name field.
- [x] Add environment picker.
- [x] Save metadata only after successful connection.
- [x] Ensure raw password is routed only through credential store.
- [x] Run `swift test` and app smoke.
- [x] Commit directly to `main`.

## Task 4: Saved connection picker/list

**Goal:** Make switching environments/connections practical.

Files:
- Create/modify saved connection view components.

Steps:
- [x] Show saved connections with environment badges.
- [x] Add connect and delete actions.
- [x] Confirm/delete safely for destructive local deletion.
- [x] Run tests.
- [x] Commit directly to `main`.

## Task 5: Production warning

**Goal:** Make production connections visually obvious.

Files:
- Modify `Sources/LithePGApp/WorkspaceView.swift`
- Modify `Tests/LithePGAppTests/AppStateTests.swift`

Steps:
- [x] Track active connection environment.
- [x] Show red warning banner when environment is production.
- [x] Include environment in connection label/status.
- [x] Run tests.
- [x] Commit directly to `main`.

## Task 6: Query history opt-in

**Goal:** Persist useful query history without result rows.

Files:
- Modify `Sources/LithePGApp/AppState.swift`
- Create query history UI component/tests.

Steps:
- [x] Add opt-in setting/state.
- [x] Append history on query completion when enabled.
- [x] Store SQL, timestamp, elapsed, success flag, summary only.
- [x] Add clear-all action.
- [x] Run tests.
- [x] Commit directly to `main`.

## Task 7: v0.3 dogfood receipt

Steps:
- [x] Update `docs/dogfood-log.md` with saved-connection/history smoke evidence.
- [x] Verify `swift test`, live dogfood smokes, and GitHub CI.
- [ ] Tag `v0.3` only when exit criteria are usable.
