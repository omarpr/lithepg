import SwiftUI

@main
struct LithePGApp: App {
    var body: some Scene {
        WindowGroup("LithePG") {
            Text("LithePG — empty shell")
                .frame(minWidth: 640, minHeight: 480)
        }
    }
}
