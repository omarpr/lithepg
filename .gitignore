# macOS
.DS_Store

# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.build/
/Packages
.swiftpm/
.netrc

# CocoaPods / Carthage
Pods/
Carthage/Build/

# IDE
.vscode/
.idea/

# Build artifacts
DerivedData/
dist/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Node / JS tooling artifacts outside the intentional `webapp/` project. Keep
# stray agent/browser installs out of the repo so `git add -A` never sweeps
# foreign npm content into the LithePG history.
node_modules/
package.json
package-lock.json
pnpm-lock.yaml
yarn.lock

!webapp/package.json
!webapp/package-lock.json
